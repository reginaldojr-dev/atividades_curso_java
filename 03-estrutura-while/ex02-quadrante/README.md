# Ex02 — Quadrante (while)

Ler pares `(x, y)` e imprimir: `primeiro`, `segundo`, `terceiro`, `quarto`. Termina se **x==0** ou **y==0**.

### Rodar
```bash
mvn -q clean compile
mvn -q exec:java
```
